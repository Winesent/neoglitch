# Neoglitch — импланты будущего 🧠⚡

Административный сервис для управления заявками на нейроимпланты.  
Реализован как часть лабораторной работы по курсу бэкенд-разработки.

> ⚠️ **Фронтенд (HTML/CSS/JS) был сгенерирован с помощью ИИ** и служит исключительно для демонстрации и удобства тестирования. Основной фокус проекта — бэкенд.

---

## 🚀 Функционал

- Просмотр списка заявок  
- Добавление новой заявки  
- Редактирование заявки (включая перенос даты)  
- Удаление заявки  
- Валидация:  
  - Дата услуги **должна быть строго в будущем**  
  - Услуга должна быть выбрана из предопределённого списка  
- Данные хранятся в локальной SQLite-базе (`data.db`)

---

## 🛠 Технологии

- **Бэкенд**: FastAPI (асинхронный)
- **ORM**: SQLAlchemy 2.0 (асинхронный режим)
- **Валидация**: Pydantic v2
- **База данных**: SQLite (локальный файл `data.db`)
- **Тесты**: pytest + FastAPI TestClient
- **Фронтенд**: HTML + CSS + JavaScript (сгенерирован ИИ)

---

## Запуск через Docker
```bash
docker build -t neoglitch .
docker run -d -p 8000:8000 --name neoglitch-app neoglitch
http://localhost:8000
```
## Запуск локальный
```bash
pip install -r requirements.txt
uvicorn main:app --reload
http://localhost:8000
```
